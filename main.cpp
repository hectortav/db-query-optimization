#include "functions.h"

//using namespace std;
//tuple is in std :/

int main(void)
{
    srand(time(NULL));
    relation R,S;

    /*R.num_tuples = 10;
    R.tuples = new tuple[R.num_tuples];
    re_ordered(&R);*/
}
